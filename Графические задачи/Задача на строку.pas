var
S:string;
count:integer;
i:integer;
error:string;
begin
writeln('count:');
readln(count);
if(count>=0) then
S:='lower bound'
else
S:=error;
for i:=0 to count do
begin
inc(count,1);
end;
writeln(s[i]);
end.
